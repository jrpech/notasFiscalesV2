import 'package:flutter/foundation.dart';
import 'package:flutter/gestures.dart';
import 'package:flutter/material.dart';
import 'package:notas_fiscales/keys.dart';
import 'package:notas_fiscales/models/models.dart';

/*class SalesItem extends StatelessWidget {
  final BranchSale sales;

  SalesItem({
    Key key,
    @required this.sales,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Dismissible(
      key: NotasFiscalesAppKeys.booksItem(sales.id),
      child: ListTile(
        title: Hero(
          tag: '${sales.id}__heroTag',
          child: Container(
            width: MediaQuery.of(context).size.width,
            child: Text(
              sales.title,
              style: Theme.of(context).textTheme.title,
            ),
          ),
        ),
      ),
    );
  }
}


*/